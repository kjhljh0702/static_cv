// Optional authoring-tool exports. No account or remote asset is required for the default CV.
// Vectary: export a GLB, put it in res/, and set vectaryModel to './res/your-model.glb'.
// Jitter: export a WebM/MP4, put it in res/, and set jitterVideo below.
// The video must be decorative, muted, and accompanied by a poster for reduced motion.
export const media = { vectaryModel: null, jitterVideo: null, jitterPoster: null };
